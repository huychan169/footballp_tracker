✅ 1. Luôn re-OCR stable player theo chu kỳ
if stable_cached and frame_idx % stable_refresh_stride != 0:
    skip


Ví dụ:

stable_refresh_stride = 20

✅ 2. Nếu pose batch fail → fallback sang per-player pose

Không thể bắt batch pose chạy tốt trong mọi trường hợp.
Giải pháp:

if pose_results[idx].valid == False:
    pose_crop = self.pose_model.predict_single()

✅ 3. Tăng chất lượng crop trong batch

Chuẩn hóa crop size trước khi đưa vào OCR.

Bổ sung padding logic:

pad = int(w * 0.15)

✅ 4. Điều chỉnh vote logic

Nếu miss_count > 3 → cưỡng bức OCR lại.

Nếu OCR return None quá nhiều → không skip.

✅ 5. Batch size linh hoạt

Không nhất thiết batch tất cả player.

Chỉ batch top 8–12 player cần OCR.
